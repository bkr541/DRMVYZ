// Placeholder — transport controls currently live inside LiveVisualPreview.
// This component will own the bottom bar once that extraction happens.

export function BottomPerformanceBar() {
  return null
}

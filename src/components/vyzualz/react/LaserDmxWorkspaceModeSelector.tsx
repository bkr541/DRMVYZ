/**
 * Legacy shim retained for patches that still import the workspace selector.
 * LaserDMX is locked to Beam Matrix, so no user-facing workspace switch renders.
 */
export function LaserDmxWorkspaceModeSelector() {
  return null
}
